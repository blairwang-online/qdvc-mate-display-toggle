# 2026-06-29 Conversation with Claude

## 1. User request

On GNOME (and Windows), the keyboard shortcut Super+P (Windows key + P) invokes a popup that toggles the screen display mode:

Mirror | Join Displays | External Only | Built-in Only

There is no equivalent on MATE. Would you be able to write a Python program (GTK3) that could do this? I could then set up a custom shortcut that pulls it up with Super+P. When it pops up, left and right arrow keys change which option is highlighted, and then pressing ENTER changes the display mode.

## 2. LLM response

Here's a GTK3 Python program that does this using `xrandr`:

```python
#!/usr/bin/env python3
import gi
gi.require_version('Gtk', '3.0')
from gi.repository import Gtk, Gdk
import subprocess
import re


def get_outputs():
    """Return (connected_outputs, primary). Connected = plugged in."""
    out = subprocess.check_output(['xrandr', '--query']).decode()
    connected = []
    primary = None
    for line in out.splitlines():
        m = re.match(r'^(\S+)\s+connected\s*(primary)?', line)
        if m:
            connected.append(m.group(1))
            if m.group(2):
                primary = m.group(1)
    if primary is None and connected:
        primary = connected[0]
    return connected, primary


def pick_internal(connected):
    """Heuristic: internal panel is usually eDP/LVDS/DSI."""
    for name in connected:
        if re.match(r'(eDP|LVDS|DSI)', name, re.I):
            return name
    return connected[0] if connected else None


def apply_mode(mode):
    connected, primary = get_outputs()
    if len(connected) < 1:
        return
    internal = pick_internal(connected)
    externals = [o for o in connected if o != internal]

    cmd = ['xrandr']

    if mode == 'Mirror':
        # Mirror everything onto the internal (or first) output position.
        base = internal or connected[0]
        for o in connected:
            cmd += ['--output', o, '--auto', '--same-as', base]

    elif mode == 'Join Displays':
        prev = None
        for o in connected:
            cmd += ['--output', o, '--auto']
            if prev:
                cmd += ['--right-of', prev]
            prev = o

    elif mode == 'External Only':
        if not externals:
            return
        for o in externals:
            cmd += ['--output', o, '--auto']
        cmd += ['--output', internal, '--off']
        # primary on first external
        cmd += ['--output', externals[0], '--primary']

    elif mode == 'Built-in Only':
        cmd += ['--output', internal, '--auto', '--primary']
        for o in externals:
            cmd += ['--output', o, '--off']

    subprocess.run(cmd)


class DisplayPopup(Gtk.Window):
    MODES = ['Mirror', 'Join Displays', 'External Only', 'Built-in Only']

    def __init__(self):
        super().__init__(type=Gtk.WindowType.POPUP)
        self.set_position(Gtk.WindowPosition.CENTER_ALWAYS)
        self.set_decorated(False)
        self.set_keep_above(True)

        self.selected = 1  # default highlight "Join Displays"
        self.buttons = []

        frame = Gtk.Frame()
        box = Gtk.Box(orientation=Gtk.Orientation.HORIZONTAL, spacing=12)
        box.set_margin_top(20)
        box.set_margin_bottom(20)
        box.set_margin_start(20)
        box.set_margin_end(20)

        for i, mode in enumerate(self.MODES):
            lbl = Gtk.Label(label=mode)
            lbl.set_size_request(120, 80)
            eb = Gtk.EventBox()
            eb.add(lbl)
            self.buttons.append(eb)
            box.pack_start(eb, True, True, 0)

        frame.add(box)
        self.add(frame)

        self.connect('key-press-event', self.on_key)
        self.connect('focus-out-event', lambda *a: Gtk.main_quit())
        self.update_highlight()

    def update_highlight(self):
        for i, eb in enumerate(self.buttons):
            ctx = eb.get_style_context()
            if i == self.selected:
                eb.override_background_color(
                    Gtk.StateFlags.NORMAL, Gdk.RGBA(0.2, 0.5, 0.9, 1))
            else:
                eb.override_background_color(
                    Gtk.StateFlags.NORMAL, Gdk.RGBA(0, 0, 0, 0))

    def on_key(self, widget, event):
        key = event.keyval
        if key in (Gdk.KEY_Right, Gdk.KEY_Tab):
            self.selected = (self.selected + 1) % len(self.MODES)
            self.update_highlight()
        elif key == Gdk.KEY_Left:
            self.selected = (self.selected - 1) % len(self.MODES)
            self.update_highlight()
        elif key in (Gdk.KEY_Return, Gdk.KEY_KP_Enter):
            mode = self.MODES[self.selected]
            self.hide()
            apply_mode(mode)
            Gtk.main_quit()
        elif key == Gdk.KEY_Escape:
            Gtk.main_quit()
        return True


def main():
    win = DisplayPopup()
    win.show_all()
    win.grab_focus()
    # Ensure keyboard focus on a POPUP window.
    win.get_window().focus(Gdk.CURRENT_TIME)
    Gtk.main()


if __name__ == '__main__':
    main()
```

Save as `~/bin/display-switcher.py`, make executable (`chmod +x`), then bind `python3 ~/bin/display-switcher.py` to Super+P in MATE's keyboard shortcuts.

Notes:
- Uses `xrandr`, so this assumes X11 (MATE's default), not Wayland.
- Internal-panel detection is a heuristic (eDP/LVDS/DSI). If your laptop names its panel differently, hardcode `internal` in `pick_internal`.
- "External Only"/"Built-in Only" do nothing if there's no external/internal to switch to.
- If the POPUP window won't grab keyboard focus reliably on your setup, change `type=Gtk.WindowType.POPUP` to `TOPLEVEL` and keep `set_decorated(False)`.

Want me to add monitor resolution detection or arrange "Join Displays" by actual physical position instead of left-to-right order?

_Committed as 15ebfafab1af86063bbb04162ad84a06e7fb122d_

## 3. User request

From this point onwards, do not place the script as a code fence, instead please provide it as a file.

A few requests:

1. Indeed, the POPUP window doesn't grab keyboard focus reliably (arrow keys do not work). Please apply the fix you suggested above.
2. Give the window a standard GTK titlebar with minimise, close, etc.
3. Make each of the 4 options a GTK button that I can click on.
4. Visibly label the 4 options with a large 1, 2, 3 and 4 and make it so that typing one of those keys highlights the corresponding option (but does not activate it immediately).

## 4. LLM response

Done. Changes applied:

1. **Focus fix** — now a `TOPLEVEL` window with `present()` on show, so it reliably grabs keyboard focus and the arrow keys work.
2. **Standard titlebar** — `set_decorated(False)` removed and a title set, so you get the normal GTK titlebar with minimise/close.
3. **Clickable buttons** — each option is a `Gtk.Button` with a `clicked` handler that applies the mode immediately on click.
4. **Big 1–4 labels + number-key selection** — each button shows a large bold number above its name. Pressing `1`–`4` (top row or keypad) highlights the matching option without activating; highlighting works via `grab_focus()`, so the focus ring shows the selection. `Enter` then activates.

Left/Right/Tab still cycle the selection, and Esc cancels.

One thing to flag: I'm using `grab_focus()` to indicate the highlighted option, which relies on your GTK theme drawing a visible focus ring on buttons. Most themes do, but if yours doesn't make it obvious enough, I can switch to explicitly styling the selected button (e.g. a colored border via a CSS provider) so the highlight is unmistakable. Want me to do that?

_Committed as 10ba0ce1366caa15d5d43fddb5a33aaaca80e841_

## 5. User request

Great work. Everything now works except Mirror (option 1) - it has no effect. I've tested it both coming from single-display mode and from extended display (join displays) (option 2). Are you able to fix, or if not fix, help me figure out why it is not working?

## 6. LLM response

The most likely cause: mirroring with `--same-as` only works if both outputs are actually set to the **same resolution**. `--auto` picks each display's *preferred* (native) mode independently, so if your internal panel and external monitor have different native resolutions, xrandr silently fails to apply the mirror (it can't overlay two different-sized framebuffers at the same origin).

GNOME/Windows "Mirror" handles this by picking a common resolution both displays support. Let me fix the script to do the same.I'll rewrite the script to (a) parse each output's available modes, (b) for Mirror, find a resolution all connected displays share and set them all to it explicitly, and (c) surface any xrandr error so failures aren't silent.Let me verify the script is syntactically valid:Fixed. Here's what was wrong and what I changed.

**Why Mirror did nothing:** the old code used `--auto` on every output, which makes each display pick its *own* native resolution. To mirror, all outputs have to share one framebuffer at the same origin and resolution — if your panel is, say, 1920×1200 and your monitor is 2560×1440, xrandr can't overlay them and quietly bails. (Note it failed silently because the old `subprocess.run` didn't check the return code, so you got no error.)

**What I changed:**
- Added mode parsing — `get_outputs()` now collects every resolution each output supports.
- Added `common_mode()` — finds the largest resolution that *all* connected displays support, and Mirror now sets every output explicitly to that mode at position `0x0` with `--same-as`. This is the same trick GNOME/Windows use.
- Errors now surface — any failed xrandr call pops up a dialog with the stderr message and the exact command, so you'll never get a silent no-op again.

The other three modes are unchanged.

Two things worth knowing:

If your two displays share **no** common resolution at all (rare, but possible), `common_mode()` returns `None` and it falls back to `--auto`, which will likely fail again — but now you'll see the error dialog telling you so. If that happens, the usual workaround is scaled mirroring (`--scale`/`--fb`), which is more involved; tell me and I'll add it.

To debug if it still misbehaves, run it from a terminal rather than the shortcut so you can see output, and you can also run `xrandr --query` and check the indented mode lines — paste me the output for your two displays and I can confirm whether a shared mode exists.

_Committed as 9b3438bb3526afc84ac612aa8c2e1428fbb44e2a_

## 7. User request

The latest version of the code is now online at https://github.com/blairwang-online/qdvc-mate-display-toggle - please fetch a copy of that and replace your working version.

Next, please make the following UI changes: above all the 4 options should be a suitable icon - I am thinking the MATE "Display Preferences" icon - and an explanation text telling the user they can either use arrow keys or number keys and hit ENTER _or_ click the desired option.

## 8. LLM response

That's the repo page. Let me fetch the raw `display-switcher.py` file directly.Got it. Let me view the full fetched version so I know exactly what I'm working from.The fetched version is essentially the same as my working copy. I'll make it the new working version and add the icon + instruction text above the buttons.Now I'll add a header section above the buttons: the MATE Display Preferences icon and the instruction text. The standard icon name in MATE/freedesktop themes is `preferences-desktop-display` (with `mate-preferences-desktop-display` as a fallback), loaded from the theme via `Gtk.Image.new_from_icon_name`.Now the button row's margins moved to `outer`, and the button box needs to be added to `outer` instead of directly to the window. Let me fix the assembly:Done. The fetched GitHub copy is now the working version (it was identical to my last edit, so nothing was lost), and I've added a header above the four buttons:

- **Icon** — a 64px `Gtk.Image` that tries `mate-preferences-desktop-display` first, then falls back to the generic `preferences-desktop-display`, then `video-display`, picking whichever your icon theme actually has. This avoids showing a broken/missing image if the MATE-specific name isn't present.
- **Instruction text** — a centered, word-wrapping label reading: *Use the arrow keys or number keys 1–4 to choose, then press Enter — or simply click an option.*

Structurally, I wrapped everything in an outer vertical box (icon → instructions → button row) and moved the window margins onto it.

One note: if none of the three icon names exist in your theme, the `Gtk.Image` just stays empty (no crash, no broken-icon glyph). Run it once from a terminal to confirm the MATE icon shows; if it comes up blank, tell me your icon theme and I'll find the right name, or I can point it at the actual icon file on disk instead of relying on theme lookup.

_Committed as 3b6afb5ea7e34de3ececebb56565daa4c613945a_

## 9. User request

Good start. Please use the 48x48 icon instead (it's a bit sharper), and arrange it so that it is on the left, with the label text on the right. Allow the label text to wrap, it would look nicer if a bit narrower and taller (easier for a human user to scan).

## 10. LLM response

Done. The header is now a horizontal box: 48px icon on the left (vertically centered), instruction text on the right.

For the text, I set `max_width_chars`/`width_chars` to 28 and turned on wrapping, so it breaks across more lines into a narrower, taller block. It's also left-aligned (`xalign=0.0`) and vertically centered against the icon, which reads more naturally than centered justification when sitting beside an icon.

If 28 characters feels too wide or too narrow once you see it, that single number controls the column width — nudge it down for a taller, skinnier block or up for a wider one.

_Committed as 49cc1631f42132fa45c8c6addafc4d01cb1090ea_

## 11. User request

I tried to change set_max_width_chars and set_width_chars to smaller numbers, and nothing changed. Is the setting actually working?

## 12. LLM response

Good catch — this is a known GTK quirk, not your imagination.

`width_chars` and `max_width_chars` only take effect when the label is actually being asked to size itself around that constraint. In your layout the label is packed with `expand=True, fill=True` into a horizontal box, and the window is `set_resizable(False)` and sized by its contents. The real width is being driven by the button row below (four 140px buttons + spacing ≈ 620px), so the header gets that whole width to play with. Since there's far more room than 28 characters needs, the label never has to wrap, and lowering the number changes nothing because the constraint is never the binding one.

In other words, the setting *is* working — it's just not the thing deciding the width. The button row is.

To actually control the wrap width you need to force the label to a hard width rather than expressing a preference. Two reliable options:

The simplest is to stop letting it expand and give it an explicit pixel width request:

```python
header.pack_start(instructions, False, False, 0)   # was True, True
instructions.set_size_request(220, -1)             # hard width, natural height
```

With `set_size_request`, the label is told "you are 220px wide, wrap to fit," and height grows as needed. Adjust 220 to taste.

If you'd rather keep using character counts, the other approach is to also call:

```python
instructions.set_line_wrap_mode(Pango.WrapMode.WORD)
instructions.set_max_width_chars(24)
```

…but you'd still need the label *not* to be the expanding child, otherwise the extra space wins again. The pixel `set_size_request` route is the one that just works regardless of what the buttons want.

Want me to apply the `set_size_request` fix to the file? If so, tell me your preferred width (or I'll use 220px) and whether you'd like the icon+text header centered above the wider button row or left-aligned.

## 13. User request

Yes, please make appropriate updates so that the width actually adjusts. Let's start with 220px as you suggested. Don't have that floating around the code as a magic number; but instead declare it as a constant at the top of the file.

## 14. LLM response

Done. Changes:

- Added `INSTRUCTION_WIDTH_PX = 220` as a module-level constant near the top, with a comment noting that lower = narrower/taller.
- Replaced the char-count hints (`set_width_chars`/`set_max_width_chars`) with `set_size_request(INSTRUCTION_WIDTH_PX, -1)`, which forces a hard pixel width so the label actually wraps to it.
- Changed the label's packing from `True, True` to `False, False` so it no longer expands to absorb the button row's extra width — that was what made the char-count setting a no-op before.

Now adjusting `INSTRUCTION_WIDTH_PX` will visibly change the wrap width. The `-1` height means it grows as many lines as it needs.

_Committed as fb10e7805e4d433bb5110029bff8906c5ba138b7_

## 15. User request

Sadly it is still not working. To be clear, the section above the 4 options should look like this:

whitespace of width X .. icon .. label (wrapped to 2+ lines) ... whitespace of width X

In other words, the icon + label are centered. Perhaps we should just set X to some amount (say, 100 pixels) and then the icon and label could reflow?

## 16. LLM response

I see the issue. The real problem is the layout intent. You want the icon+label as a centered unit with equal whitespace on both sides, and the label to wrap. The cleanest way: center the `header` box horizontally (so it doesn't stretch to the window width), and constrain the label's wrap width so it actually breaks onto multiple lines.

The reason `set_size_request` wasn't visibly wrapping is that without `set_max_width_chars`, GTK's label measures its *natural* (single-line) width as its preferred size, and since the header box was getting the full window width, the label took its full natural width and never wrapped. The fix is to combine the pixel size request with `max_width_chars` so the label reports a small preferred width, and to stop the header from expanding so it centers.

Let me switch the approach: set the constant to the side whitespace value as you suggested, center the header, and cap the label's wrap.Now let me restructure the header. The approach: wrap the header in a horizontal box with `HEADER_SIDE_PADDING_PX` margins on left and right, center it, and let the label wrap to whatever width remains. To make the label wrap reliably, I'll set `max_width_chars` to a modest value so GTK reports a small preferred width.Now let me check whether there's a leftover `header.pack_start(instructions...)` line below that I need to clean up:There's a duplicate `header.pack_start(instructions...)` line. Let me remove it.Done, and I also caught a bug: a duplicate `pack_start(instructions...)` line had crept in from the earlier edits — removed it.

What changed and why it should now behave:

- The constant is now `HEADER_SIDE_PADDING_PX = 100`, representing the whitespace on each side, as you suggested.
- The header box is centered (`halign=CENTER`) with `margin_start`/`margin_end` of 100px. Centering stops the box from stretching to the full window width — that stretching was the root cause of the label never needing to wrap.
- The label now uses `set_max_width_chars(24)` instead of a pixel `size_request`. This makes GTK report a *small* preferred width for the label, which is what actually forces wrapping. The pixel `size_request` alone didn't do that because the label still advertised its full single-line natural width.

So the result is: 100px gap, then icon + wrapped label as a centered unit, then 100px gap.

Two knobs to tune once you see it: `HEADER_SIDE_PADDING_PX` controls the side gaps, and `set_max_width_chars(24)` controls how aggressively the label wraps (lower = narrower/taller). If the line still won't break, lower the `24` first — that's the setting that governs wrap width here. Want me to expose that `24` as a named constant too, alongside the padding one?

_Committed as 0de5c602aa225a62d97216426b2888196ebb3c48_

## 17. User request

The latest version of the code is now online at https://github.com/blairwang-online/qdvc-mate-display-toggle - please fetch a copy of that and replace your working version.

Next, I would like the program to detect the current setting on startup. If only external display is activated: then we are in mode 3. If only built-in display is activated: then we are in mode 4. Otherwise, to differentiate between modes 1 and 2, inspect the current arrangement and make an educated guess.

The detected current setting should be the default highlighted button on program launch.

Please also introduce a GTK status bar that indicates to the user what the current setting is heuristically guessed to be.

## 18. LLM response

The fetched version has some changes from yours (padding now 60, a `HEADER_TEXT_LABEL_SIZE = 48` constant). I'll adopt it as the working version, then add the detection logic and status bar.

To detect the current mode, I need each output's active state and geometry. `xrandr --query` shows active outputs with a `WxH+X+Y` geometry on the connected line, so I'll extend `get_outputs()` to capture that.Now I'll extend `get_outputs()` to also capture the active geometry of each output (which tells us whether it's on, and where). I'll return a fourth dict, `geometry`, mapping output name → `(w, h, x, y)` or `None` if off.Now `apply_mode` calls `get_outputs()` and unpacks 3 values — I need to update that to 4.Now I'll add a `detect_current_mode()` function that returns an index 0–3 and a short description. Let me place it after `pick_internal`.Now I'll wire detection into `__init__` (set `self.selected` from the detected mode) and add a `Gtk.Statusbar` at the bottom of `outer`.Now add the status bar. Let me view the assembly section to insert it correctly.Let me verify the detection logic against simulated `xrandr` output for the key scenarios, since I can't run a real X server here.All five scenarios resolve correctly: join→1, mirror→0, external-only→2, built-in-only→3, and mirror with differing sizes→0.Done. Fetched the GitHub copy (it had your tweaks: padding 60, the `HEADER_TEXT_LABEL_SIZE = 48` constant) and made it the working version, then added detection and a status bar.

How detection works:

- `get_outputs()` now also returns a `geometry` dict — for each connected output, its active `(w, h, x, y)` if it's switched on, or `None` if off. xrandr only prints a `WxH+X+Y` on the output line when that output is active, which is the reliable "is it on" signal.
- `detect_current_mode()` returns a `(index, description)` matching your numbering: only external on → 3 (index 2), only internal on → 4 (index 3). When two or more are active it inspects their origins: a shared top-left `(x, y)` means they're overlaid → Mirror; different origins mean they're placed side by side → Join Displays.
- `self.selected` is set from the detected index, so the matching button is the default highlight on launch.

The `Gtk.Statusbar` at the bottom shows e.g. *Current setting: Extended displays (joined)* or *Mirrored displays*, etc.

I verified the parsing/decision logic against five simulated xrandr layouts (join, mirror, mirror-with-different-sizes, external-only, built-in-only) and all resolve to the right mode — but that's simulated output, so do sanity-check it against your actual machine in each real state.

One honest caveat on the Mirror-vs-Join guess: it relies on origin position. If you ever set up an "extended" arrangement where the second display happens to sit exactly at `+0+0` too (unusual, but possible with manual xrandr), it would read as Mirror. The origin test is the best heuristic available from xrandr alone; if you want it tighter, I could also require matching resolution for a Mirror verdict, though that would misfire on genuine mirror setups with differing native sizes. Want me to adjust the tie-breaker, or leave it as is?

_Committed as 0e39a6299a1a4ab37f7d111483a432ce01375f5f_

## 19. User request

A few usability/maintainability requests:

1. Centre the text in the status bar. Also, make it the normal size (same as the label at the top), but in italics.
2. Update the wording of the status bar to indicate that the current setting is auto-detected / guessed based on heuristics / something like that.
3. The wording of the status bar and the wording of the buttons is slightly inconsistent. For consistency, use the same wording, I suggest the following: Mirror / Extend / External Only / Built-in Only
4. Define the four possible options as an enum, and their sequencing as a List. So that in future, if we want to change the sequencing, we just need to rearrange the List.

## 20. LLM response

This is a meaningful refactor, so let me view the current file in full to plan the changes coherently.This touches detection, apply, and the UI all through the string-based mode names, so the cleanest approach is to introduce an `enum` for the modes and a `MODE_ORDER` list, then thread the enum through everywhere. Let me rewrite the file coherently rather than piecemeal — I'll build it in the writable scratch dir first.

The design:
- `Mode(Enum)` with members `MIRROR`, `EXTEND`, `EXTERNAL_ONLY`, `BUILTIN_ONLY`, each carrying its display label (the single consistent wording).
- `MODE_ORDER = [...]` — the sequencing; rearranging this list reorders buttons and renumbers keys automatically.
- `detect_current_mode()` returns a `Mode`.
- `apply_mode()` takes a `Mode`.
- Status bar wording notes it's auto-detected, centered, normal size, italic.A concern about styling the status bar message via Pango markup: `Gtk.Statusbar.push()` does not interpret markup even if the label has `use_markup` set, because it sets the text with `set_text()`, not `set_markup()`. So the `<i>` tags would show literally. Let me verify the structure and handle it properly — set the label text directly rather than relying on `push` to honor markup.The italics won't render reliably through `push()`. Rather than fight the Statusbar's internal label, I'll set the markup on that label directly (and still call `push` so the context machinery is consistent, but the markup is what shows). Actually the clean fix: skip `push` for display and set markup on the label myself. Let me also confirm `get_message_area()` exists in GTK3 (it does, since 2.20). Let me rework that block.Now let me verify the GTK widget assumptions actually hold by running the relevant parts headlessly (Xvfb), so I'm not shipping a guess about the Statusbar internals.GTK3 is now available. Let me run a headless test under Xvfb that builds the window, stubbing `xrandr` so detection runs, and assert the Statusbar label internals and markup work as expected.Everything verified live: detection returns `EXTEND`, the status label is a real `Gtk.Label`, the `<i>` markup is parsed (text shows without tags, `use_markup` True), it's center-aligned, the selected index correctly maps to the detected mode, and there are 4 buttons. Let me also confirm the "External Only" and "Built-in Only" detection and that rearranging `MODE_ORDER` reorders things.All correct. Copying the verified file to outputs.All four done, and I verified the UI live under a headless X server (Xvfb) with a stubbed xrandr rather than guessing at the GTK internals.

1. **Status bar styling** — centered (`halign=CENTER` + center justify), normal size (it inherits the default font, same as the header label), and italic. Note on *how*: `Gtk.Statusbar.push()` escapes markup, so `<i>` tags would have shown literally. I instead reach into the statusbar's message area, grab its internal `Gtk.Label`, and set markup on it directly. The live test confirmed the label parses the markup (`use_markup` is True and the text renders without visible tags).

2. **Wording** — now reads *"Auto-detected current setting (best guess): …"* to make clear it's a heuristic guess, not authoritative.

3. **Consistent wording** — buttons and status bar both draw from a single source: the `Mode` enum's label. The four labels are Mirror / Extend / External Only / Built-in Only, used everywhere (buttons, status bar, error dialog).

4. **Enum + ordering list** — added `class Mode(Enum)` with each member carrying its display label, and `MODE_ORDER = [...]` defining sequence. Buttons, number-key bindings, detection default, and click/Enter handlers all derive from `MODE_ORDER`, so reordering that one list reorders the whole UI. I confirmed the buttons render in list order and the detected mode maps to the right index.

One thing I simplified while refactoring: the old detection returned a separate human-readable description string (e.g. "Extended displays (joined)"); now it returns a `Mode` and the status bar shows the consistent label ("Extend"), per your request #3. If you'd ever want the richer phrasing back in the status bar while keeping the button labels terse, that's an easy addition — say the word.

_Committed as 90bf03fa00b823f540790c8bb9017953f8a8ae7d_

## 21. User request

Now, documentation:

1. Please provide a very brief README.md intended as a brief summary/cover page for GitHub.
2. Please provide a detailed MAINTENANCE.md, technical information for a human or AI worker who needs to maintain this codebase in future.

## 22. LLM response

I'll write both docs. Let me view the final file once more to keep details accurate.Both docs are ready.

**README.md** — a short cover page: one-line purpose, a table of the four modes, how to run and bind it to Super+P, requirements, and a pointer to the maintenance doc.

**MAINTENANCE.md** — the detailed technical reference, organised so a future maintainer can navigate by section. It covers the hard environmental assumptions (X11-only, the `xrandr` text-format dependency, GTK 3 vs 4), the `Mode` enum / `MODE_ORDER` as the main extension point with change recipes, how the `xrandr` parsing and on/off detection actually work, the Mirror-vs-Extend heuristic and its known limitation, the `apply_mode` logic (including why Mirror needs a shared resolution and why errors are surfaced), and a "gotchas" rundown of the GTK quirks that bit us during development — the TOPLEVEL-vs-POPUP focus issue, the label-wrapping interaction, the Statusbar markup-escaping workaround, and the hardcoded 1–4 key map that's the one thing not auto-scaling with `MODE_ORDER`. It ends with a headless-testing guide and a quick change-recipe cheat sheet.

I deliberately documented the development history's hard-won lessons (the silent Mirror failure, the focus bug, the wrapping fix) as explicit "don't undo this" notes, since those are exactly the things a maintainer might otherwise "tidy" and reintroduce.

A couple of things worth flagging for accuracy: the README's requirements list assumes a Debian/Ubuntu-family package naming (`python3-gi`, `gir1.2-gtk-3.0`) — if you're targeting other distros you may want to add their equivalents. Also, both docs omit a licence; GitHub will nudge you for one, so you may want to add a `LICENSE` file and a one-line licence mention in the README. Want me to add a licence section and a starter `LICENSE` file?
